import { layoutPropsWithChildren } from '@/lib/server/utils/slots'
import './moodle-simple-email-access-signup.style.scss'

export default async function MoodleSimpleEmailAccessSignupLayout(props: layoutPropsWithChildren) {
  return props.children
}
