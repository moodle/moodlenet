import { layoutPropsWithChildren } from '@/lib/server/utils/slots'
import './layout.ciccio-signup.scss'

export default async function CiccioSignupLayout(props: layoutPropsWithChildren) {
  return props.children
}
