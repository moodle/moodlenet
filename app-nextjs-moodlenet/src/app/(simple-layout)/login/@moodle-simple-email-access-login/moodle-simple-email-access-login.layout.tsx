import { layoutPropsWithChildren } from '@/lib/server/utils/slots'
import './moodle-simple-email-access-login.style.scss'

export default async function MoodleSimpleEmailAccessLoginLayout(props: layoutPropsWithChildren) {
  return props.children
}
