import { layoutPropsWithChildren } from '@/lib/server/utils/slots'
import './layout.ciccio-login.scss'

export default async function CiccioLoginLayout(props: layoutPropsWithChildren) {
  return props.children
}
