type Bookmarked implements IEdge {
  id: ID!
  _created: Timestamp!
}

## integrations
union Edge = Bookmarked
enum EdgeType {
  Bookmarked
}
input CreateEdgeInput {
  Bookmarked: Empty
}
input EditEdgeInput {
  Bookmarked: Empty
}
