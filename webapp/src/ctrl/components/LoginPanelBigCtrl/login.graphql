mutation login($username: String!, $password: String!) {
  createSession(username: $username, password: $password) {
    username
    email
    accountId
    jwt
    user {
      _id
      displayName
    }
  }
}
