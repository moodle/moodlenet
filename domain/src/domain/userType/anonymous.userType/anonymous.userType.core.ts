import { access } from './access.context/access.context.core'
import type { anonymous as anonymous_def } from './anonymous.userType'
export const anonymous: moo.def.core.userType<anonymous_def> = {
  access,
}
