import { userAccountConfigs } from '../types'

export const DEFAULT_USER_ACCOUNT_CONFIGS: userAccountConfigs = {
  schema: {
    eduDraftsOverrides: {
      collection: {},
      resource: {},
    },
  },
}
