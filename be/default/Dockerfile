# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build moodle-be-default`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t moodle-be-default`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system be-default && \
          adduser --system -G be-default be-default

COPY dist/be/default be-default/
RUN chown -R be-default:be-default .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix be-default --omit=dev -f install

CMD [ "node", "be-default" ]
