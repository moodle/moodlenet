type User implements GraphVertex {
  _id: ID!
  displayName: String!
}

input CreateUserInput {
  name: String
}

type Query {
  user(_id: ID!): User
}

type Mutation {
  createUser(user: CreateUserInput!): User!
}
