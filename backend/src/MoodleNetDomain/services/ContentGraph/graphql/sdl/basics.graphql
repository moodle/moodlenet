interface GraphVertex {
  _id: ID!
}
interface GraphEdge {
  _id: ID!
  cursor: String!
  node: GraphVertex!
}
type PageInfo {
  endCursor: String
  hasNextPage: Boolean
  hasPreviousPage: Boolean
  startCursor: String
}
interface Page {
  pageInfo: PageInfo!
  edges: [GraphEdge!]!
}

input PageInput {
  first: Int
  after: String
  before: String
  last: Int
}
