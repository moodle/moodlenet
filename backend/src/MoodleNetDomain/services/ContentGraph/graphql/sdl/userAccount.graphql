type SessionAccount @key(selectionSet: "{ username }") {
  username: String!
  user: User!
}
type Query {
  getSessionAccountUser(username: String!): SessionAccount
    @merge(keyField: "username")
}
