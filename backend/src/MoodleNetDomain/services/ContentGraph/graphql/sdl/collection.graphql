type Collection implements GraphVertex {
  _id: ID!
  name: String!
}

type Query {
  collection(_id: ID!): Collection
}

input CreateCollectionInput {
  name: String!
}

type Mutation {
  createCollection(collection: CreateCollectionInput): Collection!
}
