interface Vertex {
  _id: ID!
}

interface Edge {
  _id: ID!
  _from: Vertex
  _to: Vertex
}

enum CursorDir {
  after
  before
  middle
}
input InputCursor {
  _id: ID!
  dir: CursorDir!
}
input PageInput {
  limit: Int
  cursor: InputCursor
}
