type UserFollowsUser implements Edge {
  _id: ID!
  _from: User
  _to: User
}

type User {
  followsUsers(page: PageInput): [UserFollowsUser!]!
  followers(page: PageInput): [UserFollowsUser!]!
}

type Mutation {
  followUser(userId: ID): UserFollowsUser
}
