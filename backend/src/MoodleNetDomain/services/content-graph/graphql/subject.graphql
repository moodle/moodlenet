interface SubjectVertex {
  id: ID!
  icon: String!
  name: String!
  summary: String!
}

type Subject implements SubjectVertex & Vertex & WithIcon & WithName & WithSummary {
  id: ID!
  icon: String!
  name: String!
  summary: String!
}

extend type Query {
  subject(id: ID!): Subject
}
