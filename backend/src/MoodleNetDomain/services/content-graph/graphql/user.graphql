type User implements Vertex {
  _id: ID!
  name: String!
}

input CreateUserInput {
  name: String
}

type Query {
  user(_id: ID!): User
}

type Mutation {
  createUser(user: CreateUserInput!): User!
}
