type UserFollowsSubject implements Edge {
  _id: ID!
  _from: User
  _to: Subject
}

type User {
  followsSubjects(page: PageInput): [UserFollowsSubject!]!
}

type Subject {
  followers(page: PageInput): [UserFollowsSubject!]!
}

type Mutation {
  followSubject(subjectId: ID): UserFollowsSubject
}
