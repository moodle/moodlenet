{
  "name": "backend-monolith",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "backend/monolith/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/backend/monolith",
        "format": ["cjs"],
        "bundle": false,
        "main": "backend/monolith/src/main.ts",
        "tsConfig": "backend/monolith/tsconfig.app.json",
        "assets": ["backend/monolith/src/assets"],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          }
        }
      },
      "configurations": {
        "migrate": {
          "main": "backend/monolith/src/migrate.ts"
        },
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            }
          }
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "backend-monolith:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "backend-monolith:build:development"
        },
        "production": {
          "buildTarget": "backend-monolith:build:production"
        },
        "migrate": {
          "buildTarget": "backend-monolith:build:migrate"
        }
      }
    }
  }
}
