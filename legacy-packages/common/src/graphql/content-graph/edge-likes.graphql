type Likes implements IEdge {
  id: ID!
  _created: Timestamp!
}

## integrations
union Edge = Likes
enum EdgeType {
  Likes
}
input CreateEdgeInput {
  Likes: Empty
}
input EditEdgeInput {
  Likes: Empty
}
