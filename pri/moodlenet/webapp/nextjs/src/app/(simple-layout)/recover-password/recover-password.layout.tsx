import { layoutPropsWithChildren } from '@/lib/server/utils/slots'
import './recover-password.style.scss'

export default async function RecoverPasswordLayout(props: layoutPropsWithChildren) {
  return props.children
}
