import { layoutPropsWithChildren } from '../../../../lib/server/utils/slots'
import './login-email-pwd.style.scss'

export default async function LoginEmailPwdLayout(props: layoutPropsWithChildren) {
  return props.children
}
