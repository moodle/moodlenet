# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build pri-file-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pri-file-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pri-file-server && \
          adduser --system -G pri-file-server pri-file-server

COPY dist/pri/file-server pri-file-server/
RUN chown -R pri-file-server:pri-file-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pri-file-server --omit=dev -f install

CMD [ "node", "pri-file-server" ]
