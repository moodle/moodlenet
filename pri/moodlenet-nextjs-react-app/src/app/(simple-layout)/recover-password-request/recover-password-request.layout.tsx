import { layoutPropsWithChildren } from '../../../lib/server/utils/slots'
import './recover-password-request.style.scss'

export default async function RecoverPasswordRequestLayout(props: layoutPropsWithChildren) {
  return props.children
}
