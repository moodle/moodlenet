import { layoutPropsWithChildren } from '../../../../lib/server/utils/slots'
import './signup-email-pwd.style.scss'

export default async function SignupEmailPwdLayout(props: layoutPropsWithChildren) {
  return props.children
}
