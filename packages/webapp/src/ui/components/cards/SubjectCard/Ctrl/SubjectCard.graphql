query SubjectCard($id: ID!) {
  node(id: $id) {
    ... on IContentNode {
      name
      icon
    }
    ... on INode {
      id
      _organization {
        domain
        color
      }
    }
  }
}
