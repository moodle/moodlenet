query localInstance($id: ID = "Organization/") {
  node(id: $id) {
    ...LocalInstance
  }
}

fragment LocalInstance on Organization {
  id
  name
  description
  subtitle
  logo
  smallLogo
  color
  domain
}
