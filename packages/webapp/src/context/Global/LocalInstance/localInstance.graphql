query localInstance($id: ID = "Organization/") {
  node(id: $id) {
    ... on Organization {
      id
      name
      description
      intro
      logo
      image
      color
      domain
    }
  }
}
