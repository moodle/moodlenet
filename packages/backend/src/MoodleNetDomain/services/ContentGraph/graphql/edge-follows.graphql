type Follows implements IEdge {
  _id: ID!
  _meta: EdgeMeta
}

## integrations
union Edge = Follows
enum EdgeType {
  Follows
}
type RelCountMap {
  Follows: RelCount
}
input CreateEdgeInput {
  Follows: Empty
}
input UpdateEdgeInput {
  Follows: Empty
}
