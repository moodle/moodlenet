type Follows implements IEdge {
  _id: ID!
}

type RelCountMap {
  Follows: RelCount
}
union Edge = Follows
enum EdgeType {
  Follows
}

input CreateEdgeInput {
  Follows: Empty
}

input UpdateEdgeInput {
  Follows: Empty
}
