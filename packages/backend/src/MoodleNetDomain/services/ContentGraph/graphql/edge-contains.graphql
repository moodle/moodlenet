type Contains implements IEdge {
  _id: ID!
  _meta: EdgeMeta
}

## integrations
union Edge = Contains
enum EdgeType {
  Contains
}
type RelCountMap {
  Contains: RelCount
}
input CreateEdgeInput {
  Contains: Empty
}
input UpdateEdgeInput {
  Contains: Empty
}
