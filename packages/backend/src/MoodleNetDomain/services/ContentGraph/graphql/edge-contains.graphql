type Contains implements IEdge {
  _id: ID!
}

type RelCountMap {
  Contains: RelCount
}
union Edge = Contains
enum EdgeType {
  Contains
}

input CreateEdgeInput {
  Contains: Empty
}

input UpdateEdgeInput {
  Contains: Empty
}
