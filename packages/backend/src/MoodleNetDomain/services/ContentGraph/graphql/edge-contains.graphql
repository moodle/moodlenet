type Contains implements IEdge {
  id: ID!
}

## integrations
union Edge = Contains
enum EdgeType {
  Contains
}
input CreateEdgeInput {
  Contains: Empty
}
input UpdateEdgeInput {
  Contains: Empty
}
