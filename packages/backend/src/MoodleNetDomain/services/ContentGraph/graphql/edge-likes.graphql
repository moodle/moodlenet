type Likes implements IEdge {
  _id: ID!
  _meta: EdgeMeta
}

type RelCountMap {
  Likes: RelCount
}
union Edge = Likes
enum EdgeType {
  Likes
}

input CreateEdgeInput {
  Likes: Empty
}

input UpdateEdgeInput {
  Likes: Empty
}
