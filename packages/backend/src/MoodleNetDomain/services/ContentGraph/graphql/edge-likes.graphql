type Likes implements IEdge {
  _id: ID!
  _meta: EdgeMeta
}

## integrations
union Edge = Likes
enum EdgeType {
  Likes
}
type RelCountMap {
  Likes: RelCount
}
input CreateEdgeInput {
  Likes: Empty
}
input UpdateEdgeInput {
  Likes: Empty
}
