scalar Cursor

interface IContentNode {
  name: String! # TODO: rename to title
  summary: String! # TODO: rename to excerpt
  icon: String
}

interface INode {
  id: ID!
}

interface IEdge {
  id: ID
}
