type Collection implements IContentNode & INode {
  _id: ID!
  name: String!
  summary: String!
  icon: String
  _meta: NodeMeta!
  _rel(edge: EdgeTypeInput!, page: PaginationInput): RelPage!
}

input CreateCollectionInput {
  name: String!
  summary: String!
  icon: String
}

input UpdateCollectionInput {
  name: String
  summary: String!
  icon: String
}

# integrations
union Node = Collection
enum NodeType {
  Collection
}
type RelCountTargetMap {
  Collection: Int
}
input CreateNodeInput {
  Collection: CreateCollectionInput
}
input UpdateNodeInput {
  Collection: UpdateCollectionInput
}
