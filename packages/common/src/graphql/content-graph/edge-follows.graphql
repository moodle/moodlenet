type Follows implements IEdge {
  id: ID!
  _created: DateTime!
}

## integrations
union Edge = Follows
enum EdgeType {
  Follows
}
input CreateEdgeInput {
  Follows: Empty
}
input UpdateEdgeInput {
  Follows: Empty
}
