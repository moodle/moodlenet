type HasOpBadge implements IEdge {
  id: ID!
  _created: DateTime!
}

## integrations
union Edge = HasOpBadge
enum EdgeType {
  HasOpBadge
}
input CreateEdgeInput {
  HasOpBadge: Empty
}
input UpdateEdgeInput {
  HasOpBadge: Empty
}
