type Collection implements INode {
  image: AssetRef
  # INode
  id: ID!
  name: String!
  description: String!
  _published: Boolean!
  _rel(type: EdgeType!, target: NodeType!, inverse: Boolean, page: PaginationInput, targetIds: [ID!]): RelPage!
  _relCount(type: EdgeType!, target: NodeType!, inverse: Boolean): Int!
}

# integrations
union Node = Collection
enum NodeType {
  Collection
}

input CreateCollectionInput {
  name: String!
  description: String!
  image: AssetRefInput!
  _published: Boolean!
}
input CreateNodeInput {
  Collection: CreateCollectionInput
}

input EditCollectionInput {
  name: String!
  visibility: String!
  description: String!
  image: AssetRefInput
  _published: Boolean!
}
input EditNodeInput {
  Collection: EditCollectionInput
}
