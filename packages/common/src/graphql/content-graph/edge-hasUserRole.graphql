type HasUserRole implements IEdge {
  id: ID!
  _created: Timestamp!
}

## integrations
union Edge = HasUserRole
enum EdgeType {
  HasUserRole
}
input CreateEdgeInput {
  HasUserRole: Empty
}
input UpdateEdgeInput {
  HasUserRole: Empty
}
