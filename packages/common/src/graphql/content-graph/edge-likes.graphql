type Likes implements IEdge {
  id: ID!
  _created: GlyphByAt!
  _lastEdited: GlyphByAt
}

## integrations
union Edge = Likes
enum EdgeType {
  Likes
}
input CreateEdgeInput {
  Likes: Empty
}
input UpdateEdgeInput {
  Likes: Empty
}
