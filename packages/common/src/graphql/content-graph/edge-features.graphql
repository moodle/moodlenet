type Features implements IEdge {
  id: ID!
  _created: Timestamp!
}

## integrations
union Edge = Features
enum EdgeType {
  Features
}
input CreateEdgeInput {
  Features: Empty
}
input UpdateEdgeInput {
  Features: Empty
}
