type Pinned implements IEdge {
  id: ID!
  _created: DateTime!
}

## integrations
union Edge = Pinned
enum EdgeType {
  Pinned
}
input CreateEdgeInput {
  Pinned: Empty
}
input UpdateEdgeInput {
  Pinned: Empty
}
