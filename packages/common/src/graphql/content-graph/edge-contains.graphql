type Contains implements IEdge {
  id: ID!
  _created: GlyphByAt!
  _lastEdited: GlyphByAt
}

## integrations
union Edge = Contains
enum EdgeType {
  Contains
}
input CreateEdgeInput {
  Contains: Empty
}
input UpdateEdgeInput {
  Contains: Empty
}
